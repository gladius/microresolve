/* auto-generated by NAPI-RS — regenerate with: napi build --platform --dts index.d.ts */
/* eslint-disable */

export interface ResolveMatch {
  id: string
  score: number
}

export interface PhraseResult {
  added: boolean
  newTerms: Array<string>
  redundant: boolean
  warning?: string | null
}

export declare class Router {
  constructor()
  /** Resolve a natural language query to matching intents. Returns matches sorted by score descending. */
  resolve(query: string, threshold?: number | undefined | null, gap?: number | undefined | null): Array<ResolveMatch>
  /** Add an intent with seed phrases. */
  addIntent(id: string, seeds: Array<string>): void
  /** Correct a routing mistake: move query from wrongIntent to correctIntent. */
  correct(query: string, wrongIntent: string, correctIntent: string): void
  /** Set intent type: "action" or "context". */
  setIntentType(intentId: string, intentType: string): void
  /** Export router state as JSON string. */
  exportJson(): string
  /** Import router state from JSON string. */
  static importJson(json: string): Router
  /** Add a phrase with duplicate checking. */
  addPhrase(intentId: string, phrase: string, lang?: string | undefined | null): PhraseResult
  /** Remove a phrase from an intent. */
  removePhrase(intentId: string, phrase: string): boolean
  /** Set intent description. */
  setDescription(intentId: string, description: string): void
  /** Get intent description. */
  getDescription(intentId: string): string
  /** Delete an intent. */
  deleteIntent(id: string): void
  /** List all intent IDs. */
  intentIds(): Array<string>
}
