//! Dataset Benchmark (stub)
//!
//! BM25 routing removed. Dataset benchmarks now require Hebbian L3 (server-side).

fn main() {
    eprintln!("dataset_benchmark: BM25 routing removed. Use the server + /api/route_multi.");
}
